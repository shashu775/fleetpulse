apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fleetpulse.fullname" . }}-test-connection"
  labels:
    {{- include "fleetpulse.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fleetpulse.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
