apiVersion: v1
kind: Service
metadata:
  name: {{ include "fleetpulse.fullname" . }}
  labels:
    {{- include "fleetpulse.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fleetpulse.selectorLabels" . | nindent 4 }}
